import styled from "styled-components";

export const HeaderWrapper = styled.header`
    flex: none;
    height: auto;
    position: sticky;
    top: 0;
    width: 100%;
    will-change: transform;
    background-color: var(--gray-1);
    border-bottom: 1px solid var(--mauve-a5);
`;